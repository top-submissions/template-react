# input:

---

# process:

make commit messages and PR for staged files
see prompts/ and .github/ for format and rules

---

# output:

multiple commit messages (w/ their own updated files directory structure) using markdown
the PR fields using markdown
updated test files that are not specified in the issue (optional/conditional)
